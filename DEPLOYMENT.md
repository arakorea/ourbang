# Deployment note

- Upload the entire folder contents to your site root.
- Make sure `assets/css/site.css` is included.
- If `assets/images/brand/ourbang-logo.png` is missing, logo will hide automatically (layout remains consistent).
